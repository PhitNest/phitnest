import '../state.dart';

import 'found_location_screen.dart';
import 'found_location_view.dart';

/**
 * Holds the dynamic content of [FoundLocationScreen]. Calls to [rebuildView] will rebuild 
 * the [FoundLocationView].
 */
class FoundLocationState extends ScreenState {}
