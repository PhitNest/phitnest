const createGym = require('./createGym');
const nearestGym = require('./nearestGym');
const nearestGyms = require('./nearestGyms');

module.exports = {
  createGym: createGym,
  nearestGym: nearestGym,
  nearestGyms: nearestGyms,
};
