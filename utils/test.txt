

class User {
  String name;
  mongoose participants;

  User({
    undefined
  })

  factory User.fromJson(Map<String, dynamic> parsedJson) =>
    User(
      parsedJson['_id'],
      undefined
    )

    bool get isGroup => participants.length > 2;
}
