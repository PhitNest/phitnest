{
  "name": "mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "flutter pub get",
    "dev": "flutter run",
    "build": "bundle install --gemfile=$BUNDLE_GEMFILE",
    "deploy": "cd ios && bundle exec fastlane ios beta \"build_name:${{ github.ref_name }}\""
  },
  "dependencies": {
    "aws-config-to-dart": "workspace:*"
  }
}
