import 'registration_event.dart';

/// This event is emitted when the user removes their currently selected profile picture.
class RetakeProfilePictureEvent extends RegistrationEvent {
  const RetakeProfilePictureEvent();
}
