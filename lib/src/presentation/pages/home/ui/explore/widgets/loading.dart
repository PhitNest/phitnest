part of home_page;

class _ExploreLoadingPage extends StatelessWidget {
  const _ExploreLoadingPage() : super();

  @override
  Widget build(BuildContext context) =>
      Center(child: CircularProgressIndicator());
}
