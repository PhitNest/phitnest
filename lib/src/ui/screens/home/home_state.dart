import '../state.dart';

import 'home_provider.dart';
import 'home_view.dart';

/**
 * Holds the dynamic content of [HomeProvider]. Calls to [rebuildView] will rebuild 
 * the [HomeView].
 */
class HomeState extends ScreenState {}
