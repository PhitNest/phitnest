/// This makes it possible for other classes to import all constants at once
export 'headers.dart';
export 'http_codes.dart';
export 'backend_routes.dart';

const requestTimeout = Duration(seconds: 10);
